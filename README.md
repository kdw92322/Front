# Vite React Auth App

A Vite + React frontend with auth views and JWT token handling.

## Features

- Login / Signup / Forgot Password views
- JWT token storage + basic auth utilities
- UI built with Tailwind CSS + shadcn/ui-style components

## Setup

```bash
npm install
npm run dev
```

Open http://localhost:5173
